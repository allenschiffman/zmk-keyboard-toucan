#include <behaviors.dtsi>
#include <dt-bindings/zmk/keys.h>
#include <dt-bindings/zmk/bt.h>
#include <dt-bindings/zmk/outputs.h>
#include <dt-bindings/zmk/pointing.h>

#define BASE    0
#define NAV     1
#define SYM     2
#define ADJ     3
#define IOS     4
#define GAME    5
#define G_SHIFT 6

/ {
    conditional_layers {
        compatible = "zmk,conditional-layers";
        tri_layer {
            if-layers = <1 2>;
            then-layer = <3>;
        };
    };

    macros {
        // Launches Steam Big Picture: Win+R -> wait 100ms -> write URI -> Enter
        steam_bp: steam_big_picture {
            compatible = "zmk,behavior-macro";
            #binding-cells = <0>;
            wait-ms = <10>;
            tap-ms = <10>;
            bindings
                = <&macro_press &kp LGUI>
                , <&macro_tap &kp R>
                , <&macro_release &kp LGUI>
                , <&macro_wait_time 100>
                , <&macro_tap &kp S &kp T &kp E &kp A &kp M &kp COLON &kp FSLH &kp FSLH &kp O &kp P &kp E &kp N &kp FSLH &kp B &kp I &kp G &kp P &kp I &kp C &kp T &kp U &kp R &kp E>
                , <&macro_wait_time 10>
                , <&macro_tap &kp RET>;
        };

        // Win11 Display Switch (Extend): Win+R -> wait 50ms -> "DisplaySwitch 3" -> Enter
        disp_ext: display_extend {
            compatible = "zmk,behavior-macro";
            #binding-cells = <0>;
            wait-ms = <10>;
            tap-ms = <10>;
            bindings
                = <&macro_press &kp LGUI>
                , <&macro_tap &kp R>
                , <&macro_release &kp LGUI>
                , <&macro_wait_time 50>
                , <&macro_tap &kp D &kp I &kp S &kp P &kp L &kp A &kp Y &kp S &kp W &kp I &kp T &kp C &kp H &kp SPACE &kp N3>
                , <&macro_wait_time 10>
                , <&macro_tap &kp RET>;
        };

        // Win11 Display Switch (External Only): Win+R -> wait 50ms -> "DisplaySwitch 4" -> Enter
        disp_ext_only: display_external_only {
            compatible = "zmk,behavior-macro";
            #binding-cells = <0>;
            wait-ms = <10>;
            tap-ms = <10>;
            bindings
                = <&macro_press &kp LGUI>
                , <&macro_tap &kp R>
                , <&macro_release &kp LGUI>
                , <&macro_wait_time 50>
                , <&macro_tap &kp D &kp I &kp S &kp P &kp L &kp A &kp Y &kp S &kp W &kp I &kp T &kp C &kp H &kp SPACE &kp N4>
                , <&macro_wait_time 10>
                , <&macro_tap &kp RET>;
        };
    };
};

/* Intercept trackpad deltas and transform them to scroll events when on Layer 6 */
&msc_input_listener {
    input-processors = <&zip_temp_layer 6 2000>;
};

/ {
    keymap {
        compatible = "zmk,keymap";

        // Layer 0: Base Layer (Standard Typist Deck)
        base {
            display-name = "BASE";
            bindings = <
                &kp TAB   &kp Q  &kp W  &kp E  &kp R  &kp T     &kp Y  &kp U  &kp I      &kp O    &kp P    &kp BSPC
                &kp LCTRL &kp A  &kp S  &kp D  &kp F  &kp G     &kp H  &kp J  &kp K      &kp L    &kp SEMI &kp SQT
                &kp LSHFT &kp Z  &kp X  &kp C  &kp V  &kp B     &kp N  &kp M  &kp COMMA  &kp DOT  &kp FSLH &kp ESC
                                 &kp LGUI &mo 1 &kp SPACE   &kp RET &mo 2 &kp RALT
            >;
        };

        // Layer 1: Navigation Layer (Includes Bluetooth Controls & Studio Unlock)
        nav {
            display-name = "NAV";
            bindings = <
                &kp TAB     &kp N1        &kp N2        &kp N3        &kp N4        &kp N5          &kp N6    &kp N7     &kp N8     &kp N9     &kp N0     &kp BSPC
                &bt BT_CLR  &bt BT_SEL 0  &bt BT_SEL 1  &bt BT_SEL 2  &bt BT_SEL 3  &bt BT_SEL 4    &kp LEFT  &kp DOWN   &kp UP     &kp RIGHT  &trans     &trans
                &kp LSHFT   &studio_unlock &trans        &trans        &trans        &trans          &trans    &trans     &trans     &trans     &trans     &trans
                                          &kp LGUI      &trans        &kp SPACE                     &kp RET   &trans     &kp RALT
            >;
        };

        // Layer 2: Symbols Layer
        sym {
            display-name = "SYM";
            bindings = <
                &kp TAB   &kp EXCL &kp AT &kp HASH &kp DLLR &kp PRCNT    &kp CARET &kp AMPS  &kp KP_MULTIPLY &kp LPAR &kp RPAR &kp BSPC
                &kp LCTRL &trans   &trans &trans   &trans   &trans       &kp MINUS &kp EQUAL &kp LBKT        &kp RBKT &kp BSLH &kp GRAVE
                &kp LSHFT &trans   &trans &trans   &trans   &trans       &kp UNDER &kp PLUS  &kp LBRC        &kp RBRC &kp PIPE &kp TILDE
                                   &kp LGUI &trans   &kp SPACE           &kp RET   &trans    &kp RALT
            >;
        };

        // Layer 3: Adjust Layer (Triggers when holding NAV + SYM)
        adj {
            display-name = "ADJ";
            bindings = <
                &mo BASE   &trans &kp F7 &kp F8 &kp F9 &kp F12    &kp C_VOL_DN &kp C_MUTE &kp C_VOL_UP &trans     &trans     &kp BSPC
                &kp LCTRL  &trans &kp F4 &kp F5 &kp F6 &kp F11    &to GAME     &to IOS    &to BASE     &trans     &trans     &trans
                &bt BT_NXT &trans &kp F1 &kp F2 &kp F3 &kp F10    &trans       &trans     &trans       &trans     &trans     &trans
                                  &kp LGUI &mo 1  &kp SPACE       &kp RET      &mo 2      &kp RALT
            >;
        };

        // Layer 4: Hardcoded iOS Layer (Command and Control Swapped)
        ios {
            display-name = "IOS";
            bindings = <
                &kp TAB   &kp Q  &kp W  &kp E  &kp R  &kp T     &kp Y     &kp U  &kp I  &kp O  &kp P  &kp BSPC
                &kp LGUI  &kp A  &kp S  &kp D  &kp F  &kp G     &kp H     &kp J  &kp K  &kp L  &kp SEMI &kp SQT
                &kp LSHFT &kp Z  &kp X  &kp C  &kp V  &kp B     &kp N     &kp M  &kp COMMA &kp DOT &kp FSLH &kp ESC
                                 &kp LCTRL &mo 1 &kp SPACE      &kp RET   &mo 2  &kp RALT
            >;
        };

        // Layer 5: Primary Gaming Overlay (Left side transparent, Right side Custom Controls)
        game {
            display-name = "GAME";
            bindings = <
                &trans    &trans     &trans     &trans     &trans     &trans    &kp LA(Z)     &kp LA(F10)   &kp LA(LS(F10))  &kp LA(F1)  &kp LA(F12)  &kp LA(F4)
                &trans    &trans     &trans     &trans     &trans     &trans    &kp LG(G)     &kp C_VOL_DN  &kp C_VOL_UP     &kp C_MUTE  &mkp RCLK    &kp LC(LS(ESC))
                &trans    &trans     &trans     &trans     &trans     &trans    &disp_ext     &disp_ext_only &kp LG(N2)      &kp LG(N4)  &kp LG(N3)   &kp LG(N1)
                                     &trans     &trans     &trans               &to BASE      &kp LG(D)     &mo G_SHIFT
            >;
        };

        // Layer 6: Shifted Gaming Overlay & Trackpad Scroll Interception
        game_shift {
            display-name = "G_SHIFT";
            bindings = <
                &trans    &trans     &trans     &trans     &trans     &trans    &kp LC(LS(I))  &kp LG(LC(O))  &kp C_PP    &kp C_NEXT  &kp C_PREV  &steam_bp
                &trans    &trans     &trans     &trans     &trans     &trans    &kp LG(EQUAL)  &kp LG(MINUS)  &kp LG(ESC) &kp LC(LS(M)) &kp LC(LS(D)) &kp LA(TAB)
                &trans    &trans     &trans     &trans     &trans     &trans    &trans         &trans         &trans      &trans      &trans      &kp LA(SPACE)
                                     &trans     &trans     &trans               &to BASE       &trans         &trans
            >;
        };

        // Layers 7, 8, & 9: Virtual Reserved Layers for ZMK Studio Compatibility
        extra_1 {
            status = "reserved";
        };

        extra_2 {
            status = "reserved";
        };

        extra_3 {
            status = "reserved";
        };
    };
};
